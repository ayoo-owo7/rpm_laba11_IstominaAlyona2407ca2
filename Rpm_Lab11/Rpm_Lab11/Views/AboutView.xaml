<UserControl x:Class="Rpm_Lab11.Views.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Rpm_Lab11.Views"
             mc:Ignorable="d" 
             Margin="30" VerticalAlignment="Center" HorizontalAlignment="Center">
    <StackPanel HorizontalAlignment="Center">
        <TextBlock Text="{Binding AppName}" FontSize="24" FontWeight="Bold" Margin="0,0,0,10" TextAlignment="Center"/>
        <TextBlock Text="{Binding Version}" FontSize="16" Foreground="Gray" TextAlignment="Center"/>
    </StackPanel>
</UserControl>
